extends ../../layouts/default
include ../../mixin/box-head
include ../../mixin/product-layout
block main
    .container.my-3
        .row
            .col-12
                +box-head("Ket qua tim kiem")
        if(products.length > 0)
            +product-grid(products)
        else
            h3(class="text-center") Khong tim thay san pham nao